# AGV TaskOn Verification API

This repo contains custom API endpoints for verifying AGV Protocol campaign tasks on [TaskOn](https://taskon.xyz).  
It’s designed for deployment on **Vercel** using serverless functions (`/api/*.js`).


